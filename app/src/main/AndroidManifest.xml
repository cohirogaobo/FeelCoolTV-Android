<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    
    <!-- 声明电视特性，不要触摸屏也能用 -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false"/>

    <!-- 新增这一行：允许你的桌面应用唤起电视里的任何第三方 APP -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    
    <application 
        android:label="很酷TV" 
        android:icon="@drawable/icon"
        android:logo="@drawable/icon"
        android:theme="@style/Theme.AppCompat.NoActionBar" 
        android:usesCleartextTraffic="true"> <!-- 允许加载 http 的内网 IP -->
        
        <!-- 主界面：WebView 容器 -->
        <activity android:name=".MainActivity" android:icon="@drawable/icon" android:logo="@drawable/icon" android:exported="true" android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- 关键修复：加入标准手机版 Launcher 标签，专治国内魔改系统不显示图标的问题 -->
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- 标准 Android TV 入口标签 -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
           </intent-filter>
        </activity>

        <!-- 播放器界面 -->
        <activity android:name=".ExoPlayerActivity" android:exported="false" android:screenOrientation="landscape"/>
    </application>
</manifest>
