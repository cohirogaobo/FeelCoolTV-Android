buildscript {
    repositories {
        gradlePluginPortal() // 新增这一行
        google()
        mavenCentral()
    }
    dependencies { 
        classpath 'com.android.tools.build:gradle:8.1.1' 
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.0" 
    }
}
allprojects { 
    repositories { 
        gradlePluginPortal() // 新增这一行
        google()
        mavenCentral() 
    } 
}
